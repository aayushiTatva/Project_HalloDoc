@{
    Layout = null;
}
@model HalloDocMVC.DBEntity.ViewModels.AdminPanel.TimeSheetModel
<div class="table-responsive my-4">
    @if (Model.TimesheetdetailsList.Count > 0)
    {
        <table class="table">
            <thead>
                <tr class="table-secondary">
                    <th>Shift Date</th>
                    <th>Shift</th>
                    <th>Nightshift Weekend</th>
                    <th>Housecall</th>
                    <th>Housecall Nights Weekend</th>
                    <th>Phone consults</th>
                    <th>Phone consults Night Weekend</th>
                    <th>Batch Testing</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.TimesheetdetailsList)
                {
                    <tr>
                        <td>@item.Timesheetdate</td>
                        <td>@item.OnCallhours</td>
                        <td>0</td>
                        <td>@item.Numberofhousecall</td>
                        <td>0</td>
                        <td>@item.Numberofphonecall</td>
                        <td>0</td>
                        <td>0</td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <h5 class="my-5 mx-2 text-muted">No record(s) found</h5>
    }

    <h3>Timesheet Reimbursement</h3>

    @if (Model.TimesheetdetailreimbursementList != null)
    {
        <table class="table my-4">
            <thead>
                <tr class="table-secondary">
                    <th>Date</th>
                    <th>Item</th>
                    <th>Amount</th>
                    <th>Bill</th>
                    <th>action</th>


                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.TimesheetdetailreimbursementList)
                {
                    <tr>
                        <td>@item.Timesheetdate</td>
                        <td>@item.Itemname</td>
                        <td>@item.Amount</td>
                        <td>@item.Bill</td>
                        <td>
                            <a target="_blank" href="/Upload/TimeSheet/@Model.Timesheeid/@item.Bill" class="btn mx-1 btn-sm btn-outline-info view-btn">
                                view
                            </a>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <h5 class="my-5 mx-2 text-muted">No record(s) found</h5>
    }
</div>